name: iic_attendance_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # Excel export
  excel: ^2.1.0
  path_provider: ^2.1.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/iic_logo.png
    - assets/images/amrita_logo.png
    - assets/icon/app_icon.png   # 🔥 APP ICON

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # 🔥 LAUNCHER ICON GENERATOR
  flutter_launcher_icons: ^0.13.1


# 🔥 APP ICON CONFIGURATION
flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#FFE6C9"
  adaptive_icon_foreground: "assets/icon/app_icon.png"